# Instrucciones para Instalar el Proyecto
npm install framer-motion
npm install sonner 
npm install  lucide-react

# Instalar los Script para Aprobar
 npm install-scripts approve sharp
 npm install-scripts approve unrs-resolver

# Instalar los componetes UI
npx shadcn@latest init
npx shadcn@latest add button input label sheet card badge checkbox avatar chart
npx shadcn@latest add textarea dialog 
npx shadcn@latest add select  
npx shadcn@latest add alert-dialog
